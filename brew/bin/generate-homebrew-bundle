#!/usr/bin/env bash

readonly global_brewfile="$HOME/.Brewfile"

echo "# Generated by script $0 at $(date)" > "$global_brewfile"
find "$HOME/.dotfiles/" -name Brewfile | sort | xargs -I {} cat "{}" >> "$global_brewfile"
