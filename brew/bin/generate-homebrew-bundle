#!/usr/bin/env fish

set global_brewfile "$HOME/.Brewfile"

echo "# Generated by script "(status filename)" at "(date) > $global_brewfile
for brewfile in (find "$HOME/.dotfiles/" -name Brewfile | sort)
    cat $brewfile >> $global_brewfile
end
